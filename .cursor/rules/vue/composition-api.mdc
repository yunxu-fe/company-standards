---
description: Vue Composition API：ref、reactive、computed 与响应式陷阱
globs: "**/*.{vue,ts}"
alwaysApply: false
---

# Composition API 与响应式

## ref 与 reactive

- **基本类型与单一引用用 `ref`**；**多个相关字段的对象状态可用 `reactive`**。团队可约定「统一用 `ref`」以降低心智负担，与项目现有代码一致即可。
- **禁止对 `reactive` 返回对象直接解构后当响应式使用**（会丢失响应性）；需要解构时用 **`toRefs`** 或 **`storeToRefs`（Pinia）**。

## 计算与派生

- **派生展示/逻辑用 `computed`**，避免在模板或 `watch` 中重复计算同一派生值。
- **`computed` 内禁止副作用**（请求、赋值外部状态）；副作用放在 `watch` / 事件处理 / 生命周期中。

## 进阶

- **大型不可变数据或仅替换引用的场景**可考虑 `shallowRef` / `shallowReactive`，并注释说明原因，避免误用导致界面不更新。
- **可复用逻辑提取为 composable**（`useXxx`），与组件解耦并便于测试。
