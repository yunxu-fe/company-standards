---
description: Vue 模板安全：v-html、动态指令与客户端环境变量
globs: "**/*.{vue,ts,js}"
alwaysApply: false
---

# 模板与安全

## v-html

- **`v-html` 仅用于可信内容或经服务端策略 / 专业消毒库处理后的 HTML**；**禁止将用户原始输入直接绑定到 `v-html`**，以防 XSS。

## 动态与注入

- **避免将用户输入用于动态组件名、动态 `is` 或拼接可执行上下文**；配置驱动时使用白名单映射而非任意字符串直达组件解析。

## 环境与外链

- **客户端可见的环境变量遵循构建工具前缀约定**（如 `VITE_`），**密钥不得进前端 bundle**；集中封装 `import.meta.env` 访问。
- **`target="_blank"` 的链接使用 `rel="noopener noreferrer"`**（或组件库等价属性），与 `react/env-and-security.mdc` 原则一致。
