---
description: Vue watch、副作用清理与生命周期约定
globs: "**/*.{vue,ts}"
alwaysApply: false
---

# watch 与生命周期

## watch

- **`watch` 数据源尽量精确**：避免不必要的 `deep: true` 带来的性能与误判；多源用数组形式并理清依赖。
- **`watchEffect` 适合依赖自动收集的简单同步逻辑**；需清晰数据来源时优先 `watch`；**在 `watchEffect` 内注册的异步或副作用须在清理函数中撤销**（若 API 支持）。

## 副作用与清理

- **`onMounted` 中注册的定时器、事件监听、订阅**，必须在 **`onUnmounted`（或 `onBeforeUnmount`）中成对清理**，防止泄漏与卸载后更新状态。
- **异步请求**：组件卸载后应忽略或 **中止请求**（`AbortController` 等），避免竞态与无效赋值。

## 与模板更新

- **避免在 `watch` 中重复设置可由 `computed` 表达的派生状态**，减少循环更新与调试难度。
