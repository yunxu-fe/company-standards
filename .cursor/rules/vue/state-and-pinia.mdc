---
description: Vue 跨组件状态与 Pinia 使用边界
globs: "**/*.{vue,ts}"
alwaysApply: false
---

# 状态与 Pinia

## 边界

- **组件局部 UI 状态留在组件或 composable**；**多路由/多深层组件共享的业务状态用 Pinia**（或项目既定方案），避免滥用 `provide/inject` 传递大型可变状态导致溯源困难。

## Store 设计

- **Store 按领域拆分**，避免单文件巨型 store；**动作（actions）内封装异步与业务规则**，组件侧以语义化方法调用，减少模板与业务逻辑耦合。
- **组件中优先使用 `storeToRefs` 解构响应式 state/getters**，避免直接解构 store 丢失响应性；方法可直接从 store 实例取。

## 服务端数据

- **服务端缓存与请求去重优先使用项目选定方案**（如 TanStack Query 的 Vue 版、官方推荐封装或 Nuxt 数据层），避免在每个组件重复 `watch` + `fetch` 且无统一错误与加载态。
