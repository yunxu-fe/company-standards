---
description: React 性能（memo、懒加载）与可访问性（语义化、键盘、ARIA）
globs: "**/*.{tsx,jsx}"
alwaysApply: false
---

# 性能

- **`React.memo` 用于 props 浅比较即可避免大量重渲染的纯展示组件**；内部仍有频繁变化 props 时 memo 收益有限，避免过早优化。
- **路由级代码分割**：使用框架推荐的 `lazy` + `Suspense`（或等价方案），并为用户提供有意义的 fallback UI。
- **避免在渲染中创建新对象/函数并作为 props 下传**（除非子组件已 memo 且需稳定引用时再配合 `useCallback`/常量对象）。

# 可访问性

- **语义化 HTML 优先**：按钮用 `<button>`，链接用 `<a href>`；禁止滥用 `<div onClick>` 充当按钮（除非补充完整键盘与 ARIA 角色，仍不推荐）。
- **表单控件必须关联标签**：使用 `<label htmlFor>` 或 `aria-label`/`aria-labelledby`（与设计稿一致）。
- **图标装饰性内容**：`aria-hidden` 或空 `alt`（随场景），避免屏幕阅读器重复朗读。
- **键盘可操作**：自定义交互组件需支持 `Tab` 聚焦、`Enter`/`Space` 触发（与原生控件行为一致）。
- **对比度与焦点可见性**：不移除可见焦点环，若自定义样式需用 `:focus-visible` 提供替代视觉。

# 错误与加载边界

- **异步边界**：在路由或数据挂接处使用 `Suspense`；**错误展示**使用错误边界（类组件 `componentDidCatch` 或框架提供的 Error Boundary API），避免整页白屏无提示。
