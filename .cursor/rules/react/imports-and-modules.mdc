---
description: React/TS 前端模块导入顺序、路径别名与禁止模式
globs: "**/*.{tsx,ts,jsx,js}"
alwaysApply: false
---

# 导入与模块

## 导入位置

- **所有 `import` 写在文件顶部**；禁止在函数体或条件内动态 `import()` 滥用（代码分割的 `import()` 仅用于路由级/大体积懒加载等明确场景，并遵循项目规范）。

## 建议顺序（与 ESLint import 插件对齐时可自动修复）

1. 外部依赖（如 `react`、`react-dom`、UI 库）
2. 内部别名路径（如 `@/components`）
3. 相对路径同级/父级
4. 类型-only 导入可使用 `import type`（若项目启用 `verbatimModuleSyntax` 或团队约定要求）

## 路径与 barrel

- **优先使用项目配置的路径别名**，避免深层 `../../../`。
- **慎用 barrel 文件（`index.ts` 重导出）**：若导致整包被打入首屏 bundle，改为直接导入具体模块或按目录拆分入口。

## 导出

- **页面/路由组件可按框架约定默认导出**；可复用 UI 与业务组件优先 **具名导出**，便于重构与 tree-shaking 友好（与项目现有约定冲突时以项目为准）。
