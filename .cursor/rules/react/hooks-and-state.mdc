---
description: React Hooks、状态拆分、自定义 Hook 与 ref 使用约定
globs: "**/*.{tsx,ts,jsx,js}"
alwaysApply: false
---

# Hooks 与状态

## 规则边界

- **Hooks 仅在函数组件顶层或自定义 Hook 内调用**；禁止在条件分支、循环或嵌套函数中调用 Hooks。
- **自定义 Hook 名称必须以 `use` 开头**，返回值用对象或元组明确语义，避免返回过长无命名元组导致调用方可读性差。

## 状态设计

- **本地 UI 状态用 `useState`/`useReducer`**；跨多层组件共享时优先 Context + 合理粒度，或项目既定的状态库（Zustand、Redux 等），避免 prop drilling 过深。
- **派生状态不要在 state 中重复存储**：能从已有 state/props 计算得出的值，用变量或 `useMemo`（仅当计算昂贵时）表达。

## useMemo / useCallback

- **默认不滥用**：在子组件已用 `React.memo` 且 props 为引用类型、或依赖项参与其他 Hook 依赖数组时，再考虑 `useCallback`/`useMemo`。
- **依赖数组与 `useEffect` 同样严谨**：与渲染结果相关的值必须列入，避免闭包陈旧值。

## ref

- **DOM 与命令式 API 用 `useRef`**；不在渲染期间写入 `ref.current` 以驱动 UI（应通过 state）；与布局/动画相关的第三方库集成按官方示例处理。

## 服务端数据（约定）

- **服务端状态与缓存优先使用项目选定方案**（如 TanStack Query、SWR 或框架自带能力），避免在组件内手写分散的 `fetch` + `useEffect` 且无统一错误/加载/重试策略。
