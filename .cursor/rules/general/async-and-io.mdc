---
description: 异步、网络与 IO：取消、超时、幂等与副作用边界
globs: "**/*.{ts,tsx,js,jsx}"
alwaysApply: false
---

# 异步与 IO

## 可取消与清理

- **长耗时或组件卸载后无意义的请求应可取消**：使用 `AbortController`（或框架封装）并在 `useEffect` 清理函数或等价生命周期中 `abort`。
- **订阅、定时器、事件监听在作用域结束时取消**，避免泄漏与卸载后 setState。

## 网络与重试

- **区分可重试错误（网络抖动）与不可重试错误（4xx 业务校验）**；重试须有上限与退避策略，禁止无限重试。

## 幂等与副作用

- **写操作明确幂等策略或去重**（表单重复提交、消息重复发送）；读操作注意缓存与失效与产品一致。
- **副作用集中、可测试**：副作用与纯计算分离，便于单测与排查。

## 敏感数据

- **日志与前端上报禁止输出令牌、密码、完整证件号**；调试信息在合并前移除或脱敏。
