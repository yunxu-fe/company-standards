---
description: 通用编码原则：命名、结构、复杂度与注释
alwaysApply: true
---

# 通用编码原则

## 命名与可读性

- **命名以意图为先**：变量/函数用完整语义，避免除业内通用缩写（如 `id`、`url`、`ctx`）外的模糊简写。
- **同一概念同一词汇**：避免 `get`/`fetch`/`load` 混用表达同一类操作（按项目词汇表统一）。

## 控制流与结构

- **优先早返回**：减少深层 `if/else` 嵌套，主路径尽量保持在浅层。
- **魔法数字/字符串提取为具名常量**（或配置/枚举），禁止在逻辑中散落无说明的字面量。
- **单函数单一职责**：过长或分支过多时拆分为小函数；单文件过长时按领域或层级拆分模块。

## 注释

- **禁止复述代码的注释**；对非显而易见的约束、历史原因、性能取舍写「为什么」。
- **公共 API、复杂算法、安全相关逻辑**须有简短说明或与类型文档（TSDoc/JSDoc）配合。

## 修改范围

- **改动聚焦需求**：避免同一次提交混入无关格式化、重命名或「顺手重构」（除非任务明确要求）。
