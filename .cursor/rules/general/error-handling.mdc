---
description: 通用错误处理：捕获、记录、用户提示与不要吞异常
globs: "**/*.{ts,tsx,js,jsx}"
alwaysApply: false
---

# 错误处理

## 捕获与传播

- **禁止空 `catch` 或仅 `console.log` 后静默成功路径**：至少记录结构化信息、上报（若项目有监控），或向调用方抛出/返回 `Result` 类型。
- **异步函数中错误须进入同一套处理链**：`async/await` 用 `try/catch` 或 `.catch()`，避免未处理的 `Promise` rejection。

## 用户可见 vs 内部

- **用户提示用友好、可操作文案**；**日志与监控保留足够上下文**（请求 id、关键参数脱敏后），禁止向用户暴露堆栈或内部实现细节。

## 类型与错误对象

- **在 TS 中 `catch` 的变量按 `unknown` 处理**，收窄后再读取 `message` 等字段；禁止假定一定是 `Error` 实例。

## 资源与失败一致性

- **失败路径须释放已占用的资源**（中止请求、关闭句柄、回滚 UI 状态），与成功路径对称思考。
