---
description: TypeScript 通用约定：类型安全、空值与公共 API
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# TypeScript 通用约定

## 类型纪律

- **禁止无正当理由的 `any`**；需宽松边界时用 `unknown` 并在使用前收窄（类型守卫、`zod`/`valibot` 等按项目选型）。
- **`as` 断言仅用于有把握的场景**（如 DOM 窄化、与外部契约对齐）；禁止用断言掩盖类型设计问题。

## 空值与集合

- **可选链 `?.` 与空值合并 `??` 优于隐式假设非空**；区分「缺省」与「假值」（`??` 与 `||` 勿混用导致 bug）。
- **集合类型明确**：优先 `ReadonlyArray` / `readonly` 于不可变场景；`Map`/`Set` 与对象索引按语义选择。

## 模块与类型导出

- **公共导出的函数与组件**：为参数与返回值提供显式类型（或与 `satisfies`、推断策略一致的项目约定）。
- **`import type` 用于纯类型导入**（在 `verbatimModuleSyntax` 或打包优化要求下必须遵守）。

## 与运行时的边界

- **外部数据（API、JSON、`localStorage`）进入类型系统前必须校验或映射**，禁止直接 `as` 成业务类型。
